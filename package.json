{
  "private": true,
  "workspaces": [
    "examples/*",
    "packages/*"
  ],
  "scripts": {
    "build": "yarn --cwd packages/temple build",
    "test": "yarn --cwd packages/temple test",
    "dev": "yarn --cwd examples/with-express dev"
  },
  "devDependencies": {
    "npm-run-all": "4.1.5"
  }
}