import { describe, it } from 'mocha';
import { expect } from 'chai';

describe('Temple Document Transpiler', () => {
  it('Should transpile document', () => {
    expect(true).to.equal(true);
  });
});