{
  "name": "@ossph/temple-ui-src",
  "version": "0.1.6",
  "private": true,
  "scripts": {
    "build": "yarn build:tsc && yarn build:styles && yarn build:ui && yarn build:files && yarn build:package",
    "build:files": "cp README.md LICENSE ../temple-ui",
    "build:package": "cp ./package.live.json ../temple-ui/package.json",
    "build:styles": "cp -rf ./src/styles ../temple-ui",
    "build:ui": "cp -rf ./src/components/ ../temple-ui/",
    "build:tsc": "tsc"
  },
  "devDependencies": {
    "@ossph/temple": "0.1.6",
    "@types/dompurify": "3.0.5",
    "@types/node": "22.1.0",
    "dompurify": "3.1.6",
    "marked": "14.1.2",
    "moment": "2.30.1",
    "typescript": "5.5.4"
  }
}